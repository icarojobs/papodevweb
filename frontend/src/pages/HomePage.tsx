import { WhatsAppLayout } from '@/features/whatsapp/WhatsAppLayout'

// Tela principal: réplica do WhatsApp Web (rail de navegação + painéis).
export function HomePage() {
  return <WhatsAppLayout />
}
